<!DOCTYPE html>
<html lang="en" id="htmlTheme">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Recovery</title>
    <script>
        const darkTheme = `<%= darkTheme %>`;
        if (darkTheme === 'on') {
            document.documentElement.classList.add('darkTheme');
        } else {
            document.documentElement.classList.remove('darkTheme');
        }
    </script>
    <link rel="stylesheet" href="/stylesheets/login-register.css">
    <link rel="stylesheet" href="/stylesheets/theme.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="icon" type="image/png" href="/images/icon.png">
    <script src="https://unpkg.com/lucide@latest"></script>
</head>
<body>
<main class="auth">
    <form id="changePasswordForm" method="POST">
        <div class="card">
            <div style="display: flex; gap: 10px;">
                <a href="/" class="card__a"><i class="fa fa-home"></i></a>
                <span class="card__title">Recovery</span>
            </div>
            <p class="card__content" style="font-family: Arial, sans-serif">
                We have sent a confirmation code to your email: <%= email %>
            </p>
            <div class="card__form">
                <p class="text">Confirmation Code</p>
                <input type="text" id="node" name="code" class="card__form-input" placeholder="Enter confirmation code" autocomplete="off" required>
                <p class="text">New password</p>
                <div class="input-wrapper">
                    <input type="password" id="password" name="password" class="card__form-input" placeholder="Enter new password" required>
                    <div class="toggle-password">
                        <i data-lucide="eye-off" class="icon" id="tp" onclick="togglePassword()"></i>
                    </div>
                </div>
                <p class="text">Confirm Password</p>
                <input type="password" id="confirmPassword" name="confirmPassword" class="card__form-input" placeholder="Confirm your password" required>
                <button type="submit" class="card__button" id="changePasswordBtn">Change Password</button>
                <button class="card__button" id="loaderButton" hidden>
                    <section class="dots-container">
                        <div class="dot"></div>
                        <div class="dot"></div>
                        <div class="dot"></div>
                        <div class="dot"></div>
                    </section>
                </button>
            </div>
        </div>
    </form>
</main>
<script src="/javascripts/auth/accountRecovery.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</body>
</html>
